# Legacy replacement

There is no CONTINUE producer, REENTRY producer, workload hint, suggestion
mask, seven-class workload target, or legacy consumer. When PARP cannot decide,
ownership returns to NATIVE, never to legacy Markov.
