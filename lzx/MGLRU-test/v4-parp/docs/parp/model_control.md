# Model control

Models are versioned Q15 payloads prepared in userspace. The debugfs mode is
available now. Versioned Generic Netlink packing exists in userspace but kernel
transport remains a safe stub pending ABI review.
