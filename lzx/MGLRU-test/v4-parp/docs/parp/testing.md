# Testing

Tests cover Q15 arithmetic, state/UNKNOWN behavior, transition lookup, file and
anonymous scoring, and observe equivalence. Python uses matching vectors.
Static checks enforce native-type and hot-path restrictions.
