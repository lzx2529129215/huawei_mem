# LZX 文档索引

`lzx` 相关文档统一收录在本目录下：

- `repository_baseline/`：仓库基线、路径策略、资产清单和阶段状态。
- `design/`：Markov、App Bind 和 Hint 计数语义等设计与修复说明。
- `doc/`：内核修改文件说明等通用技术文档。

新文档应按内容放入上述分类，避免在 `lzx/` 根目录重新创建 `doc/` 或 `design/`。
