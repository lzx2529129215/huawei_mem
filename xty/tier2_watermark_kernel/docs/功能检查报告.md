# v3 内核功能检查报告

**检查日期**: 2026年7月11日  
**内核版本**: v3 (基于 linux-hwe-6.17-6.17.0)  
**对比基准**: `D:\HUAWEI_pc\2026-07\2026-0704\0706add.md`  
**检查结果**: ✅ **全部功能一致，编译通过（退出码 0），6,765 个模块成功构建**

---

## 1. 检查方法

逐项对比 0706add.md 中描述的功能特性与 v3 源码中实际实现，验证：
- 关键文件是否存在并包含预期代码
- 关键函数/结构体/API 是否已实现
- Cgroup v1/v2 注册是否正确
- 异步回收、全局计数器、MGLRU 代际感知等功能是否到位

---

## 2. 关键文件检查

| 文件 | 0706add.md 描述 | v3 实际 | 状态 |
|------|----------------|---------|------|
| `include/linux/tier2_watermark.h` | 重写，per-memcg 结构体和 API | **174 行**，含 `struct tier2_wmark_memcg`、API 声明 | ✅ |
| `mm/tier2_watermark.c` | 扩展 +620~700 行 | **1240 行**，完整实现 | ✅ |
| `include/linux/memcontrol.h` | +3 行，添加 `tier2_wmark` 指针 | **第 325 行**: `struct tier2_wmark_memcg *tier2_wmark;` | ✅ |
| `mm/memcontrol.c` | +34~60 行，init/free 钩子 + v2 注册 | **含完整的 css_alloc/css_free 钩子 + v2 `memory_files[]` 注册** | ✅ |
| `mm/Kconfig` | 未修改 | 含 `CONFIG_TIER2_WATERMARK` 配置项 | ✅ |
| `mm/Makefile` | 未修改 | `obj-$(CONFIG_TIER2_WATERMARK) += tier2_watermark.o` | ✅ |

---

## 3. 核心功能检查

### 3.1 Per-Memcg 数据结构

**文件**: `include/linux/tier2_watermark.h` (第 60 行起)

```c
struct tier2_wmark_memcg {
    int         enabled;          /* 0=use global, 1=per-memcg */
    ...
    struct mem_cgroup *memcg;
};
```

| 功能点 | 说明 | 状态 |
|--------|------|------|
| `struct tier2_wmark_memcg` | Per-memcg 水位线数据结构 | ✅ |
| `enabled` 字段 | 控制 per-memcg 启用/禁用 | ✅ |
| `alloc_scale` / `demote_scale` | 万分比水位线系数 | ✅ |
| `below_alloc` / `below_demote` | 状态标记 | ✅ |
| `reclaim_triggered` / `reclaim_actual` / `reclaim_pages` | 回收统计 | ✅ |
| `pressure_count` | 压力事件计数器 | ✅ |
| `struct work_struct reclaim_work` | 异步回收工作项 | ✅ |

---

### 3.2 Per-Memcg API

**文件**: `include/linux/tier2_watermark.h` (第 110-127 行)

| 函数 | 说明 | 状态 |
|------|------|------|
| `tier2_wmark_memcg_alloc()` | 分配并初始化 per-memcg tier2 状态 | ✅ |
| `tier2_wmark_memcg_free()` | 释放 per-memcg tier2 状态（取消 pending work） | ✅ |
| `tier2_wmark_memcg_update()` | 基于当前 limit 更新水位线 | ✅ |
| `tier2_wmark_memcg_check()` | 检查并记录水位线状态（仅观测） | ✅ |
| `tier2_wmark_memcg_check_and_reclaim()` | 检查并触发主动回收 | ✅ |

---

### 3.3 Node-Level 全局函数

**文件**: `mm/tier2_watermark.c`

| 函数 | 说明 | 状态 |
|------|------|------|
| `tier2_wmark_enabled()` | 返回全局 tier2 启用状态 | ✅ |
| `tier2_wmark_alloc_pages_pgdat()` | 计算节点 alloc 水位线（页数） | ✅ |
| `tier2_wmark_demote_pages_pgdat()` | 计算节点 demote 水位线（页数） | ✅ |
| `tier2_wmark_below_alloc()` | 检查是否低于 alloc 水位线 | ✅ |
| `tier2_wmark_below_demote()` | 检查是否低于 demote 水位线 | ✅ |
| `tier2_wmark_record_below()` | 记录低于水位线事件 | ✅ |
| `tier2_wmark_record_reclaim_wakeup()` | 记录回收唤醒事件 | ✅ |
| `tier2_wmark_record_demote_attempt()` | 记录降级尝试 | ✅ |
| `tier2_wmark_record_demote_result()` | 记录降级结果 | ✅ |
| `tier2_wmark_update_ewma()` | 更新 EWMA 滑动平均 | ✅ |

---

### 3.4 全局 sysctl 配置

**文件**: `mm/tier2_watermark.c` (第 36-38 行)

| 变量 | 默认值 | 说明 | 状态 |
|------|--------|------|------|
| `sysctl_tier2_wmark_enabled` | 0 | 全局启用/禁用 | ✅ |
| `sysctl_tier2_alloc_scale_factor` | 100 (1.00%) | alloc 水位线系数 | ✅ |
| `sysctl_tier2_demote_scale_factor` | 300 (3.00%) | demote 水位线系数 | ✅ |

对应 `/proc/sys/vm/tier2_wmark_enabled`、`tier2_alloc_scale_factor`、`tier2_demote_scale_factor`。

---

### 3.5 全局原子计数器

**文件**: `mm/tier2_watermark.c` (第 41-48 行)

| 计数器 | 类型 | 含义 | 状态 |
|--------|------|------|------|
| `tier2_global_below_alloc` | `atomic64_t` | 全局低于 alloc 水位线次数 | ✅ |
| `tier2_global_below_demote` | `atomic64_t` | 全局低于 demote 水位线次数 | ✅ |
| `tier2_global_reclaim_actual` | `atomic64_t` | 全局实际 reclaim 次数 | ✅ |
| `tier2_global_reclaim_pages` | `atomic64_t` | 全局累计回收页数 | ✅ |

全部通过 `EXPORT_SYMBOL_GPL()` 导出，可在 `/proc/kallsyms` 中查看。

---

### 3.6 Per-Cgroup 控制文件（8个）

**Cgroup v1 注册** (`mm/tier2_watermark.c`): 通过 `cgroup_add_legacy_cftypes(&memory_cgrp_subsys, ...)` 注册  
**Cgroup v2 注册** (`mm/memcontrol.c` 第 4672 行起): 在 `memory_files[]` 中添加 `#ifdef CONFIG_TIER2_WATERMARK` 块

| 文件 | 权限 | 0706add.md | v3 实现 | 状态 |
|------|------|-----------|---------|------|
| `memory.tier2_enabled` | RW | 启用/禁用 per-memcg tier2 | ✅ 已实现 | ✅ |
| `memory.tier2_alloc_scale` | RW | alloc 水位线系数（万分比） | ✅ 已实现 | ✅ |
| `memory.tier2_demote_scale` | RW | demote 水位线系数（万分比） | ✅ 已实现 | ✅ |
| `memory.tier2_alloc_wmark` | RO | 当前 alloc 水位线（bytes） | ✅ 已实现 | ✅ |
| `memory.tier2_demote_wmark` | RO | 当前 demote 水位线（bytes） | ✅ 已实现 | ✅ |
| `memory.tier2_headroom` | RO | 当前 headroom = limit - usage | ✅ 已实现 | ✅ |
| `memory.tier2_below` | RO | `alloc=X,demote=Y` 状态 | ✅ 已实现 | ✅ |
| `memory.tier2_stats` | RO | 完整统计信息 | ✅ 已实现 | ✅ |

---

### 3.7 自动 Reclaim 触发

**文件**: `mm/memcontrol.c` (第 2465-2471 行)  
**函数**: `tier2_wmark_memcg_check_and_reclaim()` (`mm/tier2_watermark.c`)

| 功能点 | 0706add.md 描述 | v3 实现 | 状态 |
|--------|----------------|---------|------|
| Hook 位置 | `try_charge_memcg()` → `done_restock` | ✅ 第 2465 行，`done_restock` 标签处 | ✅ |
| Rate limit | 每 16 次检测触发 1 次 | ✅ 已实现 | ✅ |
| 最小回收量 | 16 pages | ✅ 已实现 | ✅ |
| GFP_NOWAIT | 不在 charge 路径中阻塞 | ✅ 已实现 | ✅ |
| MEMCG_RECLAIM_PROACTIVE | 标记为主动回收 | ✅ 已实现 | ✅ |

---

### 3.8 Workqueue 异步回收

**文件**: `mm/tier2_watermark.c` (第 677 行, 709 行)

| 功能点 | 0706add.md 描述 | v3 实现 | 状态 |
|--------|----------------|---------|------|
| `struct work_struct reclaim_work` | per-memcg 工作项 | ✅ `struct tier2_wmark_memcg` 中包含 | ✅ |
| `INIT_WORK()` | memcg 创建时初始化 | ✅ 第 709 行 | ✅ |
| `schedule_work()` | 调度异步回收 | ✅ 已实现 | ✅ |
| `cancel_work_sync()` | memcg 销毁时取消 | ✅ 第 728 行附近 | ✅ |
| `tier2_wmark_reclaim_work_fn()` | 异步回收工作函数 | ✅ 第 677 行声明 | ✅ |

**异步回收流程** (`tier2_wmark_reclaim_work_fn`):
1. 重新检查水位线状态
2. 调用 `try_to_free_mem_cgroup_pages()` 进行回收
3. 更新 `reclaim_actual++` 和 `reclaim_pages += nr_reclaimed`
4. 更新 `pressure_count++`

---

### 3.9 MGLRU 代际感知回收

| 功能点 | 说明 | 状态 |
|--------|------|------|
| 代际感知 | `try_to_free_mem_cgroup_pages()` → `lru_gen_shrink_lruvec()` 内置行为 | ✅ |
| 冷页优先 | MGLRU 从最老 generation 开始扫描回收 | ✅ |
| 无需额外改动 | tier2 正常调用即可自动受益 | ✅ |

---

### 3.10 压力计数器 (pressure_count)

**文件**: `mm/tier2_watermark.c` (第 703, 863, 922 行)

| 功能点 | 说明 | 状态 |
|--------|------|------|
| `pressure_count` | per-memcg 压力事件累计 | ✅ |
| 递增时机 | 每次 below_demote + 每次 reclaim_actual | ✅ |
| stats 导出 | 通过 `memory.tier2_stats` 显示 `pressure_count=N` | ✅ (第 1222 行) |

---

### 3.11 Rate Limiting (16:1)

| 功能点 | 说明 | 状态 |
|--------|------|------|
| 检测:回收比 | 16:1（每 16 次 below_demote 触发 1 次 reclaim） | ✅ |
| `reclaim_triggered` | 记录检测总次数 | ✅ |
| `reclaim_actual` | 仅记录实际回收次数 | ✅ |

---

### 3.12 约束条件检查

| 约束 | 0706add.md 要求 | v3 实现 | 状态 |
|------|----------------|---------|------|
| demote >= alloc | `max()` 保证 | ✅ | ✅ |
| 无限 limit 禁用 | `limit == PAGE_COUNTER_MAX` → 禁用 | ✅ | ✅ |
| enabled=0 时 no-op | 所有水位线归零 | ✅ | ✅ |
| 全局/per-cgroup 共存 | 独立配置，互不干扰 | ✅ | ✅ |

---

## 4. 编译与构建验证

| 项目 | 结果 |
|------|------|
| `make olddefconfig` | ✅ 成功 |
| `make -j$(nproc)` | ✅ 退出码 0，无错误 |
| **vmlinux** | ✅ 534 MB |
| **bzImage** | ✅ 14 MB |
| **System.map** | ✅ 8.7 MB |
| **内核模块** | ✅ **6,765 个** .ko 文件 |
| **CONFIG_TIER2_WATERMARK** | ✅ `=y` |

---

## 5. Cgroup v1 注册路径

**实现文件**: `mm/tier2_watermark.c`

通过 `cgroup_add_legacy_cftypes()` 在 `tier2_wmark_init()` 中注册到 memory cgroup subsystem：

```c
static struct cftype tier2_memcg_files[] = {
    { .name = "tier2_enabled",  .flags = CFTYPE_NOT_ON_ROOT,
      .seq_show = ..., .write = ... },
    { .name = "tier2_alloc_scale", ... },
    { .name = "tier2_demote_scale", ... },
    { .name = "tier2_alloc_wmark", ... },
    { .name = "tier2_demote_wmark", ... },
    { .name = "tier2_headroom", ... },
    { .name = "tier2_below", ... },
    { .name = "tier2_stats", ... },
    { }  /* sentinel */
};
```

**v1 路径**: `/sys/fs/cgroup/memory/<cgroup>/memory.tier2_*`

---

## 6. Cgroup v2 注册路径

**实现文件**: `mm/memcontrol.c` (第 4672 行起)

在 `memory_files[]` 数组中添加 `#ifdef CONFIG_TIER2_WATERMARK` 块，注册相同的 8 个 tier2 文件。

**v2 路径**: `/sys/fs/cgroup/<cgroup>/memory.tier2_*`

---

## 7. 功能总览

| 编号 | 功能 | 0706add.md 版本 | v3 实现 | 状态 |
|------|------|----------------|---------|------|
| 1 | Node-level 二级水位线 | #3 | ✅ 已实现 | ✅ |
| 2 | Per-memcg cgroup v1 支持 | #5 | ✅ 已实现 | ✅ |
| 3 | Per-memcg cgroup v2 支持 | #6 | ✅ 已实现 | ✅ |
| 4 | 自动 reclaim 触发 (Sync) | #7 | ✅ 已实现 | ✅ |
| 5 | Workqueue 异步回收 | #8 | ✅ 已实现 | ✅ |
| 6 | reclaim_actual + reclaim_pages 分离 | #8 | ✅ 已实现 | ✅ |
| 7 | MGLRU 代际感知回收 | #8 | ✅ 已实现 | ✅ |
| 8 | 全局 vmstat 计数器 (4个) | #8 | ✅ 已实现 | ✅ |
| 9 | pressure_count 压力通知 | #8 | ✅ 已实现 | ✅ |
| 10 | Rate limiting (16:1) | #7+ | ✅ 已实现 | ✅ |
| 11 | 全局/per-cgroup 共存 | — | ✅ 已实现 | ✅ |
| 12 | 约束条件 (demote>=alloc 等) | — | ✅ 已实现 | ✅ |

---

## 8. 结论

**v3 内核源码完全实现了 0706add.md 中描述的所有功能特性。**

- 4 个关键文件 (`tier2_watermark.h`, `tier2_watermark.c`, `memcontrol.h`, `memcontrol.c`) 均包含预期修改
- 8 个 per-cgroup 控制文件在 cgroup v1 和 v2 中均正确注册
- 自动 reclaim 触发、workqueue 异步回收、MGLRU 代际感知、全局计数器、pressure_count 等高级功能全部到位
- 内核编译完全成功（退出码 0，6,765 个模块）
- `CONFIG_TIER2_WATERMARK=y` 已正确配置

**特别注意：二级水位线功能（Tier2 Watermark）已完整实现**，包括：
- Node-level 全局水位线 (`mm/tier2_watermark.c` — `tier2_wmark_alloc_pages_pgdat()` 等)
- Per-memcg 水位线 (`struct tier2_wmark_memcg` + 8 个 cgroup 文件)
- 自动回收机制 (check_and_reclaim + workqueue 异步)
- 完整统计和观测接口 (debugfs + cgroup stats)
